import 'package:flutter/material.dart';

class RecipeMenuTest extends StatelessWidget {
  const RecipeMenuTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
